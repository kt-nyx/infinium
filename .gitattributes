* text=auto eol=lf

*.dll binary
*.exe binary
*.pdb binary
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.zip binary
*.7z binary
*.nupkg binary

# Public fixtures are byte-exact retained artifacts whose SHA-256 values are
# transitively bound by package manifests. Never rewrite or text-merge them.
test-data/public-fixtures/** -text -diff -merge -eol
