# M1 Slice 4 protocol `/4` public oracle-authorability review

Status: Hard stop; independent re-review did not pass

Recorded: 2026-08-05

Evaluator: `3693d19563c636cd2879804633ca4ce52448d2c1`

Protocol/projection: `infinium.evaluator-v2/4` /
`infinium.evaluator-v2.slice4-semantic-projection/3.0.0`

This remains the historical attestation for the failed attempt. ADR-0029 later
resolved the disclosed partial `RACE/DATA` choice, and the accepted
[Pre-B2 totality plan](../plans/slices/M1-slice-4.5-pre-B2-evidence-contract-totality.md)
now owns comprehensive public closure. The failed review is not converted into
a pass and does not authorize private work.

## Role and isolation

One fresh delegated product-blind reviewer was created with no forked turns.
Its positive allowlist contained accepted public product, architecture,
evaluation, and governance authority; the attempted construction draft; the
generic answer-free package; and only frozen evaluator `/4` public schemas,
adapter, and canonicalization mechanics at the exact evaluator commit.

The reviewer did not receive or inspect product source, product tests,
candidate diffs, candidate assemblies or output, prior oracles, private
details, rollout memory, or implementation-history summaries. It did not
access the evaluator-private repository or legacy archive. No network,
billable call, candidate execution, calibration, adaptation, comparison,
scoring, B2, C2, or Stage D operation occurred. Scratch artifacts remained
under ignored `work/m1-slice4-protocol4-authorability-review/`.

## Initial review

The reviewer independently constructed a 1,073-fact diagnostic spanning all
fifteen active families and reasoned through all ten declared mutations. It
did not report PASS because:

- anonymous unsupported field and shape events did not identify whether their
  affected members overlapped decoded contributions, changed completed counts,
  or increased admitted denominators;
- no supplied positive variant exercised retained zero-denominator coverage;
- the Windows PowerShell command failed on `ConvertFrom-Json -Depth`; and
- the PowerShell 7 command used culture sorting where ordinal sorting was
  normative, and only five of ten mutations were self-exercised.

The ignored initial report SHA-256 was
`251cb1ded0110bd5e4b965314d364a4eb33dbab3b25750a975cff27a69c4ec54`.

## Single focused correction pass

The primary made the one correction pass permitted by the plan:

- the main fixture was reset from version `1.0.0` to `1.1.0` and its gap
  members received explicit admission and decode dispositions;
- a separate answer-free zero-denominator fixture was added;
- the validator adopted ordinal sorting, removed the unsupported JSON depth
  argument, checked explicit unsupported-event members and zero rows, and
  self-exercised all ten declared mutations; and
- the same reviewer was required to overwrite the prior diagnostic and
  independently author both corrected exercises.

## Re-review result

The correction uniquely closed the original arithmetic:

- `npc-records`: denominator `5`, completed `5`, state
  `completed_with_gaps`, with two decoded members carrying the field gap;
- `race-records`: denominator `2`, completed `1`, state
  `completed_with_gaps`, with one separately identified admitted undecoded
  shape member; and
- the zero variant: 42 facts, comprising the result pair and all ten retained
  `0/0/completed` coverage rows.

PowerShell 7 produced a diagnostic PASS for 1,073 main facts across all
fifteen families, 42 zero-variant facts, ten coverage populations, zero
duplicate fact IDs, and all ten mutations rejected. This was a mechanical
diagnostic only, not an authorability or product-conformance PASS.

The required Windows PowerShell command still failed before completion because
the validator used the .NET Core-only
`String.Contains(string, StringComparison)` overload. The single correction
pass had already been consumed, so that defect was not repaired.

Most importantly, the re-review found a second material authority gap. The
new admitted but unsuccessfully decoded RACE shape member has structural
identity but no exact `DATA` occurrence count. The public draft does not choose
whether that member:

- creates an override chain;
- creates the required record-contribution taxonomy technical core;
- increases `taxonomy-subjects` from 19 to 20;
- emits allowlisted `DATA` facts and, if so, with what count; or
- remains coverage/gap-only because semantic decoding failed.

These choices produce different exact facts. The diagnostic used the final
choice only to exercise mechanics; it is not oracle truth. The ignored
re-review report SHA-256 was
`443029b9e6d73c28f8080fddb44a0b64dd60a0e6f0a2cfeea7ec09072d693085`.

## Reviewed input identities

The re-review bound these working-diff inputs before this closeout changed
their status text:

| Input | SHA-256 |
|---|---|
| Construction draft | `2c18015d66342cebdcf19ad2ef6c38c653683481f58124adeda1a0e5ad1fffbf` |
| Package README | `a58de92779cdd7eaa195763b6ca0c3ff092263ff375a80c873a950caaaac950a` |
| Main execution manifest | `ac9209afdd9f1ed140e50459d6f9c528d94d05c08c6012569730ecf2ac2a314c` |
| Main byte ledger | `a295c3c8d8066a76ba2df5cb0e1d820f7541a22212da90278491576a9fd76483` |
| Coverage ledger | `69f6c54ba892cc4f97614ae53d3af332eb740c9428b96b6ce171892164f9906f` |
| Zero execution manifest | `43a856d2ee7ec9b92b4fc470d6536b655ac6b1e6e518d38339c4462e34371b44` |
| Zero byte ledger | `ae85ccb1a6f572dfafc6de0613e03aff83a72d6c0f31b0fb2250f42d9e2136f0` |
| Validator | `81fa8a557ca4e599342fc040cb47bbff2acead4a03e7c92915bad83a69294ce0` |

Frozen evaluator mechanics were re-read from commit
`3693d19563c636cd2879804633ca4ce52448d2c1` and remained unchanged.

## Disposition

The product-blind authorability gate failed after the single permitted
correction pass. Candidate source and tests were therefore not inspected, and
the frozen conformance comparison was not reached. This result is not
documentation-only conformance, product mismatch, or an unresolved evaluator
`/4` representation gap; it is the plan's earlier second-public-authority-gap
hard stop. No product or evaluator verdict exists.

The next role is the project owner for a new milestone-plan disposition. That
role may clarify or redesign the public contract, but this task does not
authorize another correction, another reviewer, a private B2 successor,
corpus qualification, candidate execution, C2, Stage D, Slice 5, or protocol
`/5`.
