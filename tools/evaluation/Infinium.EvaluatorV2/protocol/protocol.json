{
  "protocol_id": "infinium.evaluator-v2/4",
  "version": "4.0.0",
  "retired_predecessor": {
    "protocol_id": "infinium.evaluator-v2/3",
    "version": "3.0.0",
    "evaluator_commit": "34ed0c84165e9a49f44a88ecd87cac967132ebd7",
    "status": "qualified public evaluator, superseded before a valid successor corpus because its projection required non-independently-authorable oracle values"
  },
  "scorer": { "id": "infinium.evaluator-v2.scorer", "version": "4.0.0" },
  "adapter": { "id": "infinium.evaluator-v2.slice4-reflection-adapter", "version": "4.0.0" },
  "projection": { "id": "infinium.evaluator-v2.slice4-semantic-projection", "version": "3.0.0" },
  "schemas": [
    "infinium.evaluator-v2.execution-manifest/v4",
    "infinium.evaluator-v2.prepared-comparison-manifest/v4",
    "infinium.evaluator-v2.corpus-execution-manifest/v4",
    "infinium.evaluator-v2.candidate-semantic-output/v4",
    "infinium.evaluator-v2.expected-semantic-output/v4",
    "infinium.evaluator-v2.assertion-results/v4",
    "infinium.evaluator-v2.sanitized-result/v4",
    "infinium.evaluator-v2.calibration-results/v4"
  ],
  "commands": {
    "stage_b_prepared_qualification": "compare-prepared --manifest <prepared-comparison-manifest.json> --candidate-output <prepared-candidate-output.json> --oracle <expected-output.json> --result-dir <new-directory>",
    "stage_c_single_invocation": "score-corpus --manifest <corpus-execution-manifest.json> --result-dir <new-directory>"
  },
  "projection_rules": {
    "fact_id": "slash-delimited public collection and field names; stable set identities are RFC3986-escaped path segments; ordered sequences use zero-padded ordinal indexes",
    "sets": "plugins are ordered by accepted load order; override contributions, masters, providers, and links with ordinals are ordered sequences; maps and semantic sets are sorted by their named stable identity",
    "duplicates": "duplicate fact IDs, map identities, plugin names, load orders, member IDs, provider-chain paths, and provider IDs are invalid at their owning contract boundary",
    "form_keys": "eight lowercase hexadecimal local-ID digits, colon, lowercase plugin filename with esm, esp, or esl extension",
    "typed_values": "validation follows declared value_type: integer requires a JSON number exactly representable as signed Int64, including integral decimal or exponent notation; number accepts every finite JSON number including an integral-valued token; string, boolean, and null require their matching JSON kinds; objects and arrays are invalid",
    "numbers": "semantic integer facts compare as exact signed Int64 values; semantic number facts compare as finite IEEE-754 values, so 10 and 10.0 are equal when both declare value_type number; negative zero compares equal to zero",
    "null_missing": "a projected null is an explicit typed null fact; a missing projected fact is a missing_fact product mismatch; an unexpected projected fact is an extra_fact mismatch",
    "included": "bounded result presence, plugin/provider topology, evaluator-owned record and contribution identity, override/winner, NPC/RACE/REFR semantics, link semantics, allowlisted field counts, FaceGen provider topology, evaluator-owned taxonomy semantics, coverage, and capability gaps",
    "excluded": "product-generated contribution, participant, assignment, analyzer, evidence, failure, coverage-gap, and snapshot identifiers; typed AIDT subfields; failure-code spelling; physical paths; SnapshotAuthorizedPath; source snapshot IDs; dependency fingerprints; producer/build metadata outside tuple identity; timestamps; exception messages; Reason or Message prose; display text; redundant manifest hashes and lengths; and incidental serialization fields"
  },
  "terminal_rules": {
    "PASS": "every validly admitted corpus member and assertion passes",
    "FAIL": "all members are validly admitted and at least one candidate execution, candidate-output contract, state, fact, or value assertion fails",
    "EVALUATOR_ERROR": "manifest, tuple, retained bytes, evaluator identity/dependency, oracle admission, result publication, or other evaluator infrastructure is invalid; no aggregate product verdict"
  },
  "identity": {
    "evaluator_files_fingerprint": "SHA-256 of canonical relative_path|byte_length|sha256 lines in ordinal relative-path order",
    "corpus_fingerprint": "SHA-256 binds corpus ID/version plus each canonical member ID in declared order, execution inputs, and oracle identity; member identity drift requires a new freeze",
    "executing_binary": "the actual executing Infinium.EvaluatorV2.dll must be the declared file under the declared evaluator root; the deps graph and every required public schema/protocol file must be declared and byte-matched",
    "candidate": "candidate assembly and every declared non-framework dependency are byte-matched before invocation; candidate load context permits no undeclared non-framework fallback"
  },
  "privacy": "member identities, local paths, raw candidate outputs, oracles, and assertions remain private; sanitized aggregate output exposes only tuple identities, aggregate member/assertion counts, public failure categories, and the permitted terminal stage"
}
