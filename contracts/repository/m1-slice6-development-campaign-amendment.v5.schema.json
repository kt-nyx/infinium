{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "infinium.repository.m1-slice6-development-campaign-amendment/5.0.0",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_identity", "amendment_id", "status", "prior_owner_authority", "retained_failure", "retained_receipt", "recovery", "preserved_boundaries", "authorized_at_utc"],
  "properties": {
    "schema_identity": { "const": "infinium.repository.m1-slice6-development-campaign-amendment/5.0.0" },
    "amendment_id": { "$ref": "#/$defs/id" },
    "status": { "const": "owner-authorized-credential-replacement-cleanup-recovery" },
    "prior_owner_authority": { "$ref": "#/$defs/bindingPath" },
    "retained_failure": { "$ref": "#/$defs/bindingPath" },
    "retained_receipt": {
      "type": "object",
      "additionalProperties": false,
      "required": ["path", "sha256", "sequence", "outcome", "assignment_id"],
      "properties": {
        "path": { "$ref": "#/$defs/path" },
        "sha256": { "$ref": "#/$defs/sha" },
        "sequence": { "const": 3 },
        "outcome": { "const": "FailedKnown" },
        "assignment_id": { "$ref": "#/$defs/id" }
      }
    },
    "recovery": {
      "type": "object",
      "additionalProperties": false,
      "required": ["mode", "diagnosis", "predecessor_state", "successor_generation_id", "fresh_generation_creation", "assignment_kind", "helper_launches", "provider_effect"],
      "properties": {
        "mode": { "const": "run-exact-replace-helper-from-delete-pending-and-publish-recovery-only-after-completion" },
        "diagnosis": { "const": "replacement-recovery-authority-prefix-was-not-admitted-by-masked-secret-source" },
        "predecessor_state": { "const": "ordinal1-delete-pending-unavailable-cleanup-failed" },
        "successor_generation_id": { "$ref": "#/$defs/id" },
        "fresh_generation_creation": { "const": "prohibited-already-durably-reserved" },
        "assignment_kind": { "const": "Replace" },
        "helper_launches": { "const": 1 },
        "provider_effect": { "const": "prohibited" }
      }
    },
    "preserved_boundaries": {
      "type": "object",
      "additionalProperties": false,
      "required": ["hard_budget_nano_usd", "historical_evidence_immutable", "enumeration", "exposure", "slice7"],
      "properties": {
        "hard_budget_nano_usd": { "const": 10000000000 },
        "historical_evidence_immutable": { "const": true },
        "enumeration": { "const": "prohibited" },
        "exposure": { "const": "prohibited" },
        "slice7": { "const": "not-authorized" }
      }
    },
    "authorized_at_utc": { "type": "string", "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}\\.[0-9]{7}Z$" }
  },
  "$defs": {
    "id": { "type": "string", "minLength": 1, "maxLength": 256, "pattern": "^[A-Za-z0-9._:/-]+$" },
    "sha": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
    "path": { "type": "string", "minLength": 1, "maxLength": 500, "pattern": "^(?!/)(?!.*(?:^|/)\\.\\.?/)(?!.*\\\\)[^\\r\\n]+$" },
    "bindingPath": { "type": "object", "additionalProperties": false, "required": ["id", "path", "sha256"], "properties": { "id": { "$ref": "#/$defs/id" }, "path": { "$ref": "#/$defs/path" }, "sha256": { "$ref": "#/$defs/sha" } } }
  }
}
