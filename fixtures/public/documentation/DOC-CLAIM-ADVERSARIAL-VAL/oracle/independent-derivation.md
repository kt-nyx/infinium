# Independent derivation — DOC-CLAIM-ADVERSARIAL-VAL

The answer-bearing values in this package were authored before any product comparison. The oracle author did not inspect or execute the documentation importer and did not use product output.

Offsets, fingerprints, claim-base/final-claim identities, application identity, semantic gap identities, deletion-receipt identity, and aggregate identities were independently calculated from the checked-in bytes and answer-free bindings using the public documentation stage length-prefixed SHA-256 identity rule documented in the sibling oracle. Final claims bind the cycle-safe reachable contradiction graph; the accepted application binds its exact distinct ordinal-sorted emitted evidence set. The case matrix fixes its complete accepted target, including installation snapshot and resolved input manifest. The hostile command, SQL, URL, secret-shaped string, and interpolation text are inert data. Their presence authorizes no process, database, network, credential, provider, search, Nexus, or LOOT action.

Deletion and live-source-unavailability cases consume the retained clean evidence, provide no source bytes and no newly authored claims/applications, preserve the historical revision/passage/claim/application identities, create a new retained-reuse import identity, and expose explicit deletion/unavailable-source plus replay gaps. Each aggregate contains only its current reuse import descriptor; `reused_import_id` links to the preserved clean producing import. Deleted reuse starts without copied historical gaps, records the independently derived deletion receipt, and is audit-only. Live-source unavailability retains R1 bytes and therefore has partial replay. Clean-import with deleted availability is invalid.

Aggregate evidence identities bind schema ID/version and originating run plus ordered decimal-framed lists of full revision, current-import, passage, claim, application, purpose, deletion-receipt, gap, and failure DTO descriptors. Product order is stable-ID ordinal for objects, with provider/search/Nexus/LOOT boundary order and ordinal-sorted nested sets. The deletion-receipt identity also binds the empty canonical independently-retained-payload set. Retained reuse preserves the originally published revision and passage contracts byte-for-byte: both keep `present` retention and `complete-clean` revision replay metadata. Later physical deletion is represented by its receipt, gaps, and persistence-level replay state; live-source unavailability is represented by gaps while retained bytes still permit clean replay. Descriptor text is literal UTF-8; only the outer stable-ID arguments use four-byte signed big-endian framing.

Malformed and unsupported expectations were authored from the closed import schema and exact UTF-8 boundary rules, not from observed product behavior. The independent calculator was ephemeral and product-free; no generator is retained.
