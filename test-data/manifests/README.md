# Fixture manifests

Executable fixture instances are not implemented by Slice 0 or Slice 1.
Slice 1 defines their versioned schemas and answer-isolated readers under
`contracts/json-schema` and `src/Infinium.Application`; this directory remains
reserved for later slices that instantiate the accepted manifests.
